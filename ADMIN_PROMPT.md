# Промпт для админки: Управление заказами с просроченными

## Задача

Реализовать функционал управления просроченными заказами в административной панели с визуальным выделением и фильтрацией.

## Требования к реализации

### 1. Визуальное выделение просроченных заказов

**В таблице списка заказов:**
- Просроченные заказы должны иметь красную подсветку строки или бордер
- Иконка предупреждения (⚠️) рядом с просроченными заказами
- Отображение времени просрочки: "Просрочено на N минут/часов/дней"
- Использовать поле `isOverdue: boolean` из API ответа
- Отображать `overdueMinutes` для детализации

**Цветовая схема:**
```css
/* Просроченные заказы */
.overdue-order {
  background-color: #fff5f5;
  border-left: 4px solid #dc3545;
}

/* Текст просрочки */
.overdue-badge {
  color: #dc3545;
  font-weight: 600;
}
```

### 2. Фильтр по просроченным заказам

**Добавить в панель фильтров:**
- Чекбокс или переключатель "Показать только просроченные"
- Использовать query параметр `isOverdue=true` в API запросе
- Работает совместно с другими фильтрами (статус, дата, клиент)

**Пример запроса:**
```
GET /orders?isOverdue=true&status=paid&page=1&limit=20
```

### 3. Поиск по данным клиента

**Добавить поле поиска:**
- Текстовое поле для ввода поискового запроса
- Поиск работает по:
  - Имени клиента (`customer.name`)
  - Телефону клиента (`customer.phone`)
  - Email клиента (`customer.email`)
- Использовать query параметр `customerSearch` в API запросе
- Поддержка частичного совпадения (case-insensitive)

**Пример запроса:**
```
GET /orders?customerSearch=Иван&page=1&limit=20
```

### 4. Статус заказа: просрочен / не выполнен

**В карточке заказа отображать:**
- Бейдж статуса просрочки:
  - 🔴 "ПРОСРОЧЕН" - если `isOverdue === true`
  - 🟡 "В РАБОТЕ" - если статус `assigned` или `in_progress` и не просрочен
  - 🟢 "ВЫПОЛНЕН" - если статус `done`
  - ⚪ "НОВЫЙ" - если статус `new` или `paid` и не просрочен

**Детальная информация:**
- Если просрочен: "Просрочено на X минут (с {дата scheduledAt})"
- Если не просрочен: "Выполнить до {дата scheduledAt}"

### 5. Принятие просроченных заказов курьерами

**Для администратора:**
- Просроченные заказы должны быть доступны для назначения курьеру
- В списке доступных заказов просроченные должны быть вверху с приоритетом
- Кнопка "Назначить курьера" доступна для просроченных заказов со статусом `new` или `paid`

**API endpoint для назначения:**
```
PATCH /orders/:id/take
Body: { courierId: "uuid" }
```

### 6. Завершение просроченных заказов

**Логика:**
- Курьер может завершить просроченный заказ через стандартный процесс
- Администратор может вручную изменить статус на `done`
- При завершении просроченного заказа `isOverdue` автоматически становится `false`

**API endpoints:**
```
PATCH /orders/:id/complete
Body: { courierId: "uuid" }

PATCH /orders/:id/status
Body: { status: "done" }
```

## Компоненты для реализации

### 1. Список заказов с фильтрами

```typescript
interface OrderFilters {
  status?: OrderStatus;
  isOverdue?: boolean;
  customerSearch?: string;
  startDate?: string;
  endDate?: string;
  page?: number;
  limit?: number;
}

const OrderList = () => {
  const [filters, setFilters] = useState<OrderFilters>({
    isOverdue: false,
    customerSearch: '',
    // ...
  });

  // API запрос с фильтрами
  const { data } = useQuery(['orders', filters], () =>
    api.get('/orders', { params: filters })
  );

  return (
    <div>
      <OrderFilters filters={filters} onChange={setFilters} />
      <OrderTable orders={data.orders} />
    </div>
  );
};
```

### 2. Фильтры заказов

```typescript
const OrderFilters = ({ filters, onChange }) => (
  <div className="filters">
    {/* Фильтр просроченных */}
    <label>
      <input
        type="checkbox"
        checked={filters.isOverdue || false}
        onChange={(e) =>
          onChange({ ...filters, isOverdue: e.target.checked })
        }
      />
      Только просроченные
    </label>

    {/* Поиск по клиенту */}
    <input
      type="text"
      placeholder="Поиск по имени/телефону/email клиента"
      value={filters.customerSearch || ''}
      onChange={(e) =>
        onChange({ ...filters, customerSearch: e.target.value })
      }
    />

    {/* Другие фильтры */}
  </div>
);
```

### 3. Таблица заказов

```typescript
const OrderTable = ({ orders }) => (
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Клиент</th>
        <th>Статус</th>
        <th>Просроченность</th>
        <th>Запланировано</th>
        <th>Действия</th>
      </tr>
    </thead>
    <tbody>
      {orders.map((order) => (
        <OrderRow key={order.id} order={order} />
      ))}
    </tbody>
  </table>
);
```

### 4. Строка заказа с подсветкой

```typescript
const OrderRow = ({ order }) => {
  const isOverdue = order.isOverdue;
  const overdueMinutes = order.overdueMinutes;

  return (
    <tr className={isOverdue ? 'overdue-order' : ''}>
      <td>{order.id}</td>
      <td>
        {order.customer.name}
        <br />
        <small>{order.customer.phone}</small>
      </td>
      <td>
        <StatusBadge status={order.status} />
      </td>
      <td>
        {isOverdue && (
          <span className="overdue-badge">
            ⚠️ Просрочено на {formatOverdueTime(overdueMinutes)}
          </span>
        )}
      </td>
      <td>
        {order.scheduledAt
          ? formatDateTime(order.scheduledAt)
          : 'Не указано'}
      </td>
      <td>
        <OrderActions order={order} />
      </td>
    </tr>
  );
};

const formatOverdueTime = (minutes: number) => {
  if (minutes < 60) return `${minutes} мин`;
  if (minutes < 1440) return `${Math.floor(minutes / 60)} ч`;
  return `${Math.floor(minutes / 1440)} дн`;
};
```

### 5. Бейдж статуса

```typescript
const StatusBadge = ({ status, isOverdue }) => {
  const getBadgeConfig = () => {
    if (isOverdue) {
      return { label: 'ПРОСРОЧЕН', color: 'red', icon: '🔴' };
    }
    switch (status) {
      case 'done':
        return { label: 'ВЫПОЛНЕН', color: 'green', icon: '🟢' };
      case 'in_progress':
      case 'assigned':
        return { label: 'В РАБОТЕ', color: 'yellow', icon: '🟡' };
      case 'new':
      case 'paid':
        return { label: 'НОВЫЙ', color: 'gray', icon: '⚪' };
      case 'canceled':
        return { label: 'ОТМЕНЕН', color: 'gray', icon: '⚫' };
      default:
        return { label: status, color: 'gray', icon: '' };
    }
  };

  const config = getBadgeConfig();

  return (
    <span className={`badge badge-${config.color}`}>
      {config.icon} {config.label}
    </span>
  );
};
```

## API Endpoints

### Получение списка заказов с фильтрами

```
GET /orders
Query параметры:
  - page: number (default: 1)
  - limit: number (default: 10)
  - status: OrderStatus (optional)
  - isOverdue: boolean (optional) - фильтр просроченных
  - customerSearch: string (optional) - поиск по клиенту
  - customerId: string (optional)
  - currierId: string (optional)
  - startScheduledAtDate: string (optional, ISO 8601)
  - endScheduledAtDate: string (optional, ISO 8601)

Ответ:
{
  orders: OrderResponseDto[],
  total: number
}

OrderResponseDto включает:
  - id: string
  - customer: { id, name, phone, email }
  - status: OrderStatus
  - scheduledAt: Date | null
  - isOverdue: boolean - флаг просроченности
  - overdueMinutes?: number - минуты просрочки (если просрочен)
  - ...
}
```

### Назначение курьера на заказ

```
PATCH /orders/:id/take
Body: { courierId: string }
Authorization: Bearer <token>

Ответ: OrderResponseDto
```

### Изменение статуса заказа

```
PATCH /orders/:id/status
Body: { status: OrderStatus, currierId?: string }
Authorization: Bearer <token>

Ответ: OrderResponseDto
```

### Завершение заказа курьером

```
PATCH /orders/:id/complete
Body: { courierId: string }
Authorization: Bearer <token>

Ответ: OrderResponseDto
```

## Логика просроченности

Заказ считается просроченным, если:
1. Есть `scheduledAt` (запланированное время)
2. `scheduledAt < текущее время`
3. Статус заказа НЕ `done` и НЕ `canceled`

Поле `isOverdue` вычисляется на сервере динамически и всегда актуально.

## Примеры использования

### Получить только просроченные заказы

```
GET /orders?isOverdue=true&status=paid
```

### Поиск заказов по телефону клиента

```
GET /orders?customerSearch=+79001234567
```

### Комбинированный фильтр

```
GET /orders?isOverdue=true&status=paid&customerSearch=Иван&page=1&limit=20
```

## Дополнительные рекомендации

1. **Сортировка:** Просроченные заказы должны быть первыми в списке
2. **Уведомления:** Добавить уведомления администратору о новых просроченных заказах
3. **Статистика:** Добавить дашборд с метриками:
   - Количество просроченных заказов
   - Среднее время просрочки
   - Заказы, просроченные более чем на 24 часа
4. **Экспорт:** Возможность экспортировать список просроченных заказов в Excel/CSV
5. **Автообновление:** Автоматическое обновление списка каждые 30 секунд для актуальности данных

## Технические детали

- Использовать TypeScript для типизации
- React Query или SWR для управления состоянием API
- Форматирование дат через библиотеку date-fns или dayjs
- Адаптивный дизайн для мобильных устройств
- Доступность (a11y) - поддержка клавиатурной навигации и screen readers
